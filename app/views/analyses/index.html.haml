.row
  .col-lg-12
    .page-header
      %h2 Listado de Analisis
.row  
  .col-lg-12
    %table.table.table-hover.table-striped.table-users
      %thead
        %tr
          %th.text-center Paciente
          %th.text-center Doctor
          %th.text-center Fecha de Recepcion
          %th.text-center Fecha de Entrega
          %th.text-center
      %tbody
        - @analyses.each do |analysis|
          %tr
            %td.text-center= analysis.patient_full_name
            %td.text-center= analysis.doctor_full_name
            %td.text-center= analysis.receipt_date
            %td.text-center= analysis.delivery_date
            %td= link_to 'Ver', analysis
            %td= link_to 'Editar', edit_analysis_path(analysis)
            %td= link_to 'Borrar', analysis, :method => :delete, :data => { :confirm => 'Esta seguro?' }

/= link_to 'New Analysis', new_analysis_path
